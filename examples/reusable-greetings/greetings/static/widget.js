// Served by the greetings reusable app's own embed.FS route
// (/greetings/static/widget.js), referenced from trimmedNameWidget's
// rendered HTML — no framework-level asset mechanism involved (Milestone
// 16). This is intentionally a no-op placeholder: this widget's real
// behavior (trimming whitespace) happens server-side in Parse, so this
// file only proves the asset-serving path works end to end.
